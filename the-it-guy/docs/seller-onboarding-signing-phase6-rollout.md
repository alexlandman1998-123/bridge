# Seller onboarding → signing Phase 6 rollout

Phase 6 is a pilot gate. It cannot enable global rollout. Before a named agency uses the journey, retain the evidence listed in `config/seller-onboarding-signing-phase6-rollout.json` for each required scenario.

Run `npm run check:seller-onboarding-signing-phase6-rollout`. `HOLD` is the expected result until a human approves a named-agency pilot.
