# Rentals CRM Phase 8 — Tenant Matching

Phase 8 evaluates tenant leads against the scoped rental listing inventory. The transparent score considers desired area, monthly budget and bedroom requirement, then labels listings as `strong match`, `possible match` or `review`.

Shortlisting a listing writes an internal CRM activity only. It does not alter the tenant stage, reserve a property, schedule a viewing, or submit an application.
