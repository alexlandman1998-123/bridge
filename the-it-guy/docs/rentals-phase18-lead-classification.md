# Rentals Phase 18 — Existing CRM lead classification

Rental leads remain canonical CRM leads. A Rental classification attaches vacancy, unit, source, assigned agent and Rental stage metadata; it does not create a Rental contact/lead table or alter default Sales lead queries. Database persistence is intentionally deferred until the existing CRM lead schema’s extension point is confirmed in the target environment.
