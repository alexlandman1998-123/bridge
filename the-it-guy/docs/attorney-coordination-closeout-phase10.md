# Attorney coordination Phase 10 — expansion closeout

Phase 10 verifies every approved Phase 9 wave sequentially. Each wave needs separate authorization, an exact before/add/after organisation allowlist receipt, safe default-off targeting, a working kill switch, and its full observation window before the next wave.

Every sampled action must be authenticated, successful, visible in Televent where permitted and correctly attributed. Role sample and propagation thresholds remain those approved in Phase 9, and all safety counters must stay zero. Unknown waves, cohort drift, targeting failures or safety incidents return `ROLLBACK`.

Final closeout also requires target-population evidence, a reviewed delegation register with zero orphan active grants, and named ongoing monitoring, support, security and data ownership. Safe flag defaults and rollback remain in place after approval. The checker validates immutable files only and never changes production.

Only `STEADY_STATE_APPROVED`, using the exact confirmation `ACCEPT_ATTORNEY_COORDINATION_STEADY_STATE`, completes the coordination release.
