import { Copy, Loader2, Send } from 'lucide-react'
import { useState } from 'react'
import { createPersistedRentalApplicantAccess, createPersistedRentalApplication } from '../../../../services/rentals/rentalApplicationRepository.js'
export function RentalApplicationInvitePanel({ vacancy = {}, userId = '' }) { const [link, setLink] = useState(''); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); const create = async () => { try { setSaving(true); setError(''); const application = await createPersistedRentalApplication({ organisationId: vacancy.organisationId, vacancyId: vacancy.id, unitId: vacancy.unitId, createdBy: userId }); const access = await createPersistedRentalApplicantAccess(application.id, { createdBy: userId }); setLink(`${window.location.origin}/rental-application/${access.token}`) } catch (cause) { setError(cause?.message || 'Unable to create application.') } finally { setSaving(false) } }; const copy = async () => { try { await navigator.clipboard.writeText(link) } catch { setError('Copy the link manually.') } }; return <section className="rounded-xl border bg-white p-5 shadow-sm"><h2 className="font-semibold">Applicant application</h2><p className="mt-1 text-sm text-slate-600">Create a secure draft and applicant link for this vacancy.</p>{link ? <div className="mt-3 flex gap-2"><input readOnly value={link} className="min-w-0 flex-1 rounded-lg border px-3 py-2 text-xs"/><button onClick={() => void copy()} className="rounded-lg border px-3 py-2" aria-label="Copy applicant link"><Copy className="h-4 w-4"/></button></div> : <button disabled={saving} onClick={() => void create()} className="mt-3 inline-flex items-center gap-1 rounded-lg bg-sky-700 px-3 py-2 text-sm font-semibold text-white disabled:opacity-60">{saving ? <Loader2 className="h-4 w-4 animate-spin"/> : <Send className="h-4 w-4"/>}Create applicant link</button>}{error ? <p className="mt-2 text-sm text-red-700">{error}</p> : null}</section> }
