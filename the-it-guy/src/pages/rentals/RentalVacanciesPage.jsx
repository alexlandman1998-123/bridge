import { CalendarDays, Loader2, Plus, RefreshCw } from 'lucide-react'
import { useCallback, useEffect, useMemo, useState } from 'react'
import { Link } from 'react-router-dom'
import { useWorkspace } from '../../context/WorkspaceContext'
import { listRentalVacancies } from '../../services/rentals/rentalVacancyRepository.js'
const text = (value) => String(value ?? '').trim()
function scopeFrom(context = {}) { const membership = context.currentMembership || context.organisationMembership || {}; return { organisationId: text(context.workspace?.id || membership.organisation_id || membership.organisationId), branchId: text(membership.branch_id || membership.branchId) } }
export function RentalVacanciesPage() { const workspace = useWorkspace(); const scope = useMemo(() => scopeFrom(workspace), [workspace]); const [vacancies, setVacancies] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const load = useCallback(async () => { if (!scope.organisationId) return; try { setLoading(true); setError(''); setVacancies(await listRentalVacancies(scope)) } catch (cause) { setError(cause?.message || 'Unable to load vacancies.'); setVacancies([]) } finally { setLoading(false) } }, [scope]); useEffect(() => { void load() }, [load]); return <main className="mx-auto max-w-6xl space-y-6 px-4 py-6"><header className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm font-semibold text-sky-700">Rentals / Vacancies</p><h1 className="text-2xl font-bold text-slate-900">Vacancies</h1><p className="mt-1 text-sm text-slate-600">Operational availability stays independent from marketing listings.</p></div><div className="flex gap-2"><button onClick={() => void load()} className="inline-flex items-center gap-1 rounded-lg border px-3 py-2 text-sm"><RefreshCw className="h-4 w-4"/>Refresh</button><Link to="/agent/rentals/vacancies/new" className="inline-flex items-center gap-1 rounded-lg bg-sky-700 px-3 py-2 text-sm font-semibold text-white"><Plus className="h-4 w-4"/>Create vacancy</Link></div></header>{error && <p className="rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</p>}{loading ? <div className="flex justify-center py-10"><Loader2 className="h-6 w-6 animate-spin"/></div> : vacancies.length ? <section className="grid gap-3 md:grid-cols-2">{vacancies.map((vacancy) => <Link key={vacancy.id} to={`/agent/rentals/vacancies/${vacancy.id}`} className="rounded-xl border bg-white p-4 shadow-sm hover:border-sky-300"><p className="text-xs font-semibold uppercase text-sky-700">{vacancy.status}</p><h2 className="mt-1 font-semibold">Unit {vacancy.unitId}</h2><p className="mt-2 text-sm text-slate-600">R{vacancy.askingRent.toLocaleString()} · available {vacancy.availableFrom || 'TBC'}</p></Link>)}</section> : <section className="rounded-xl border border-dashed p-10 text-center"><CalendarDays className="mx-auto h-8 w-8 text-slate-400"/><h2 className="mt-3 font-semibold">No vacancies yet</h2><p className="mt-1 text-sm text-slate-600">Create a draft vacancy from a managed unit.</p></section>}</main> }
