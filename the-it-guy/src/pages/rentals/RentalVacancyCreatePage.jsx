import { Loader2 } from 'lucide-react'
import { useCallback, useEffect, useMemo, useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { useWorkspace } from '../../context/WorkspaceContext'
import { listRentalProperties } from '../../services/rentals/rentalPropertyRepository.js'
import { listRentalUnits } from '../../services/rentals/rentalUnitRepository.js'
import { createRentalVacancy } from '../../services/rentals/rentalVacancyRepository.js'
const text = (value) => String(value ?? '').trim(); const initial = { propertyId: '', unitId: '', availableFrom: '', askingRent: 0, depositAmount: 0, leaseTermMonths: 12, vacancyReason: '' }
function scopeFrom(context = {}) { const membership = context.currentMembership || context.organisationMembership || {}; return { organisationId: text(context.workspace?.id || membership.organisation_id || membership.organisationId), branchId: text(membership.branch_id || membership.branchId), userId: text(context.profile?.id || context.userId) } }
export function RentalVacancyCreatePage() { const workspace = useWorkspace(); const scope = useMemo(() => scopeFrom(workspace), [workspace]); const navigate = useNavigate(); const [form, setForm] = useState(initial); const [properties, setProperties] = useState([]); const [units, setUnits] = useState([]); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); useEffect(() => { if (!scope.organisationId) return; void listRentalProperties(scope).then(setProperties).catch((cause) => setError(cause?.message || 'Unable to load properties.')) }, [scope]); const loadUnits = useCallback(async (propertyId) => { setUnits(propertyId ? await listRentalUnits({ propertyId }) : []) }, []); const set = (key, value) => setForm((current) => ({ ...current, [key]: value })); const chooseProperty = async (value) => { set('propertyId', value); set('unitId', ''); try { await loadUnits(value) } catch (cause) { setError(cause?.message || 'Unable to load units.') } }; const submit = async (event) => { event.preventDefault(); try { setSaving(true); setError(''); const vacancy = await createRentalVacancy({ ...scope, ...form, assignedAgentId: scope.userId, createdBy: scope.userId }); navigate(`/agent/rentals/vacancies/${vacancy.id}`) } catch (cause) { setError(cause?.message || 'Unable to create vacancy.') } finally { setSaving(false) } }; return <main className="mx-auto max-w-2xl space-y-6 px-4 py-6"><Link to="/agent/rentals/vacancies" className="text-sm font-medium text-sky-700">← Vacancies</Link><div><h1 className="text-2xl font-bold">Create vacancy</h1><p className="mt-1 text-sm text-slate-600">Starts as a draft. It cannot enter marketing until landlord and mandate readiness is confirmed.</p></div><form onSubmit={submit} className="grid gap-3 rounded-xl border bg-white p-5 shadow-sm"><select required value={form.propertyId} onChange={(event) => void chooseProperty(event.target.value)} className="rounded-lg border px-3 py-2"><option value="">Choose property</option>{properties.map((property) => <option key={property.id} value={property.id}>{property.name}</option>)}</select><select required value={form.unitId} onChange={(event) => set('unitId', event.target.value)} className="rounded-lg border px-3 py-2"><option value="">Choose unit</option>{units.map((unit) => <option key={unit.id} value={unit.id}>{unit.unitLabel}</option>)}</select><div className="grid grid-cols-2 gap-3"><input type="date" value={form.availableFrom} onChange={(event) => set('availableFrom', event.target.value)} className="rounded-lg border px-3 py-2"/><input type="number" min="0" value={form.askingRent} onChange={(event) => set('askingRent', event.target.value)} placeholder="Asking rent" className="rounded-lg border px-3 py-2"/></div><div className="grid grid-cols-2 gap-3"><input type="number" min="0" value={form.depositAmount} onChange={(event) => set('depositAmount', event.target.value)} placeholder="Deposit" className="rounded-lg border px-3 py-2"/><input type="number" min="1" value={form.leaseTermMonths} onChange={(event) => set('leaseTermMonths', event.target.value)} placeholder="Lease months" className="rounded-lg border px-3 py-2"/></div><input value={form.vacancyReason} onChange={(event) => set('vacancyReason', event.target.value)} placeholder="Vacancy reason (optional)" className="rounded-lg border px-3 py-2"/>{error && <p className="text-sm text-red-700">{error}</p>}<button disabled={saving} className="inline-flex justify-center rounded-lg bg-sky-700 px-3 py-2 text-sm font-semibold text-white disabled:opacity-60">{saving ? <Loader2 className="h-4 w-4 animate-spin"/> : 'Create draft vacancy'}</button></form></main> }
export default RentalVacancyCreatePage
