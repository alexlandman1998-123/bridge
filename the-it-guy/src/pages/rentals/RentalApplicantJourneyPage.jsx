import { Loader2, Save } from 'lucide-react'
import { useCallback, useEffect, useMemo, useState } from 'react'
import { useParams } from 'react-router-dom'

const sections = [['identity', 'About you', [['firstName', 'First name'], ['lastName', 'Last name'], ['email', 'Email'], ['phone', 'Phone']]], ['employment', 'Employment', [['employer', 'Employer'], ['role', 'Role'], ['employmentType', 'Employment type']]], ['income', 'Income', [['monthlyIncome', 'Monthly income'], ['otherIncome', 'Other monthly income']]], ['rentalHistory', 'Rental history', [['currentAddress', 'Current address'], ['landlordName', 'Current landlord'], ['reasonForMoving', 'Reason for moving']]]]
const dataObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {}
const outcomeCopy = { submitted: ['Application submitted', 'Your application is with the Arch9 rentals team for review.'], under_review: ['Application under review', 'Your application is currently being reviewed.'], approved: ['Application approved', 'Your application has been approved. The rentals team will contact you about the next steps.'], declined: ['Application outcome', 'Thank you for your application. Unfortunately, it was not successful on this occasion.'], withdrawn: ['Application withdrawn', 'This application has been withdrawn.'] }
export default function RentalApplicantJourneyPage() { const { token = '' } = useParams(); const [application, setApplication] = useState(null); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); const load = useCallback(async () => { try { setLoading(true); const response = await fetch('/api/public/rental-application', { headers: { Authorization: `Bearer ${token}` } }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error); setApplication(payload.application) } catch (cause) { setError(cause?.message || 'Unable to open application.') } finally { setLoading(false) } }, [token]); useEffect(() => { void load() }, [load]); const complete = useMemo(() => sections.filter(([key]) => Object.keys(dataObject(application?.data?.[key])).length).length, [application]); const update = (section, key, value) => setApplication((current) => ({ ...current, data: { ...current.data, [section]: { ...dataObject(current.data?.[section]), [key]: value } } })); const save = async () => { try { setSaving(true); setError(''); const response = await fetch('/api/public/rental-application', { method: 'PATCH', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify({ version: application.version, patch: application.data }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error); setApplication(payload.application) } catch (cause) { setError(cause?.message || 'Unable to save application.') } finally { setSaving(false) } }; if (loading) return <main className="mx-auto flex min-h-screen max-w-xl items-center justify-center p-6"><Loader2 className="h-6 w-6 animate-spin" /></main>; if (error || !application) return <main className="mx-auto max-w-xl p-6"><h1 className="text-xl font-bold">Application unavailable</h1><p className="mt-2 text-slate-600">{error || 'This link is invalid or has expired.'}</p></main>; if (application.status !== 'draft') { const [heading, message] = outcomeCopy[application.status] || ['Application update', 'Your application is no longer available for editing.']; return <main className="mx-auto max-w-xl p-6"><p className="text-sm font-semibold text-sky-700">Arch9 Rentals</p><h1 className="mt-1 text-2xl font-bold">{heading}</h1><p className="mt-2 text-slate-600">{message}</p></main> }; return <main className="mx-auto max-w-xl space-y-5 p-4 pb-24"><header><p className="text-sm font-semibold text-sky-700">Arch9 Rentals</p><h1 className="text-2xl font-bold">Rental application</h1><p className="mt-1 text-sm text-slate-600">{complete} of {sections.length} sections started. Your draft is saved when you choose Save.</p></header>{sections.map(([section, title, fields]) => <section key={section} className="rounded-xl border bg-white p-4 shadow-sm"><h2 className="font-semibold">{title}</h2><div className="mt-3 grid gap-3">{fields.map(([key, label]) => <label key={key} className="text-sm font-medium text-slate-700">{label}<input value={application.data?.[section]?.[key] || ''} onChange={(event) => update(section, key, event.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2 font-normal" /></label>)}</div></section>)}{error ? <p className="text-sm text-red-700">{error}</p> : null}<div className="fixed inset-x-0 bottom-0 border-t bg-white p-3"><button disabled={saving} onClick={() => void save()} className="mx-auto flex w-full max-w-xl items-center justify-center gap-2 rounded-lg bg-sky-700 px-4 py-3 font-semibold text-white disabled:opacity-60">{saving ? <Loader2 className="h-4 w-4 animate-spin" /> : <Save className="h-4 w-4" />}Save draft</button></div></main> }
