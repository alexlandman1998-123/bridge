import assert from 'node:assert/strict'; import { buildRentalOperationsSnapshot } from '../rentalOperationsDashboardModel.js'; assert.equal(buildRentalOperationsSnapshot({ properties:[{}], units:[{status:'vacant'},{status:'occupied'}], vacancies:[{status:'marketing'}], projections:[{syncStatus:'failed'}] }).projectionAttention,1); console.log('Rental operations dashboard model tests passed.')
