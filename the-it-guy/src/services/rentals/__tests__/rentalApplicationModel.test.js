import assert from'node:assert/strict';import{calculateRentalApplicationCompletion,createRentalApplicationDraft,saveRentalApplicationDraft}from'../rentalApplicationModel.js';const a=createRentalApplicationDraft({applicationId:'a',organisationId:'o',vacancyId:'v',unitId:'u',applicantPartyId:'p'});assert.equal(saveRentalApplicationDraft(a,{identity:{name:'A'}}).version,2);assert.equal(calculateRentalApplicationCompletion({...a,data:{identity:{},employment:{},income:{},rentalHistory:{}}}).percent,0);console.log('Rental application model tests passed.')
