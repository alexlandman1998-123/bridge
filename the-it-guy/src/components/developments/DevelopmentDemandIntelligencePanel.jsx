import { BarChart3, ArrowUpRight, Users } from 'lucide-react'
import Button from '../ui/Button'

const TONE = { no_stock: 'bg-[#fff0ed] text-[#c1422f]', tight: 'bg-[#fff7e8] text-[#a56413]', active: 'bg-[#eef8f1] text-[#247249]', quiet: 'bg-[#f1f5f8] text-[#64788d]' }

export default function DevelopmentDemandIntelligencePanel({ intelligence, onOpenLeads, onOpenUnits }) {
  const { rows = [], unclassifiedLeadInterest = 0, tightTypes = [] } = intelligence || {}
  return <section className="rounded-[20px] border border-[#dce5ee] bg-white p-5 shadow-[0_10px_24px_rgba(15,23,42,0.045)]"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><span className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[0.13em] text-[#355f7d]"><BarChart3 size={14} />Demand intelligence</span><h3 className="mt-2 text-[1.08rem] font-semibold tracking-[-0.025em] text-[#142132]">{tightTypes.length ? `${tightTypes.length} unit type${tightTypes.length === 1 ? '' : 's'} need attention` : 'Demand is aligned with current stock'}</h3><p className="mt-1 text-sm leading-6 text-[#6b7d93]">Lead interest compared with available stock by unit type.</p></div><div className="flex gap-2"><Button type="button" size="sm" variant="secondary" onClick={onOpenLeads}><Users size={13} />Leads</Button><Button type="button" size="sm" variant="secondary" onClick={onOpenUnits}>Stock<ArrowUpRight size={13} /></Button></div></div>{rows.length ? <div className="mt-4 overflow-hidden rounded-[14px] border border-[#e4ebf3]"><div className="grid grid-cols-[minmax(0,1fr)_76px_76px_92px] gap-2 bg-[#f7f9fc] px-3 py-2 text-[0.65rem] font-bold uppercase tracking-[0.09em] text-[#71869b]"><span>Unit type</span><span className="text-right">Leads</span><span className="text-right">Stock</span><span className="text-right">Signal</span></div>{rows.slice(0, 6).map((row) => <div key={row.type} className="grid grid-cols-[minmax(0,1fr)_76px_76px_92px] items-center gap-2 border-t border-[#edf2f7] px-3 py-2.5 text-sm"><strong className="truncate text-[#233c51]">{row.type}</strong><span className="text-right text-[#526d84]">{row.leadInterest}</span><span className="text-right text-[#526d84]">{row.available}</span><span className={`justify-self-end rounded-full px-2 py-1 text-[0.65rem] font-bold uppercase tracking-[0.06em] ${TONE[row.signal]}`}>{row.signal.replace('_', ' ')}</span></div>)}</div> : <p className="mt-4 rounded-[14px] border border-dashed border-[#d8e2ee] bg-[#fbfcfe] px-4 py-5 text-sm text-[#6b7d93]">Lead interest will appear here once buyer leads record a preferred unit type.</p>}{unclassifiedLeadInterest ? <p className="mt-3 text-xs text-[#7a8da1]">{unclassifiedLeadInterest} lead{unclassifiedLeadInterest === 1 ? '' : 's'} have no unit-type preference yet.</p> : null}</section>
}
