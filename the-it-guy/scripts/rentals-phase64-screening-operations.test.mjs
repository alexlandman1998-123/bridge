import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import{fileURLToPath}from'node:url';import{dirname,join}from'node:path';const r=join(dirname(fileURLToPath(import.meta.url)),'..'),read=p=>readFile(join(r,p),'utf8'),m=await read('../supabase/migrations/20260831102441_rental_screening_operations.sql'),p=await read('src/pages/rentals/RentalScreeningPage.jsx');for(const x of['rental_screening_templates','rental_screening_cases','rental_screening_evidence_requests','rental_record_screening_manual_outcome','automatic_decision'])assert.ok(m.includes(x));assert.ok(p.includes('Screening operations'));console.log('Rentals Phase 64 screening operations checks passed.')
