import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises'; const [app,page,model]=await Promise.all(['src/App.jsx','src/pages/rentals/RentalCalendarPage.jsx','src/services/rentals/rentalCalendarModel.js'].map((file)=>readFile(new URL(`../${file}`,import.meta.url),'utf8'))); assert.match(app,/<RentalCalendarPage\s*\/>/); assert.match(page,/Rental Calendar/); assert.match(model,/occupation/); console.log('Rental calendar phase 16 integration checks passed.')
