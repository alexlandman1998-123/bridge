# Manual data review — FICA declaration canonical pack

Migration: `20260913190000_fica_declaration_canonical_pack_phase3.sql`  
Reviewed and approved by: Alex  
Reviewed at: 2026-09-14T16:49:07Z

## Intended outcome

Create or converge two canonical document definitions (`buyer_fica_declaration`
and `seller_fica_declaration`) plus five fixed-ID document-requirement rules.
The migration defines request metadata only. It does not write customer case
data, uploaded documents, verification status, or a verification decision.

## Live data result

The read-only audit found neither canonical definition and none of the five
fixed rule IDs in production or staging. Both tables exist in production, and
their conflict targets are present: the definition key is the primary key and
the rule ID is the primary key.

## Idempotency decision

`apply_original_after_dependency_check`

Both inserts use `ON CONFLICT` against those stable keys. Rerunning therefore
converges the canonical configuration instead of adding duplicate rules. The
definition metadata is merged with any pre-existing metadata, and the migration
does not set a document, party, FICA case, or review state to verified.

## Required execution checks

1. Confirm the document-pack foreign-key values are present in staging.
2. Apply only this original migration after its ordered staging dependencies.
3. Re-run `sql/supabase-phase7-fica-declaration-data-audit.sql`; all 7 rows
   must be present with `canonical_match = true`.
4. Run the canonical-document verification data guard and the FICA manual-pack
   service test before recording the staging ledger.

Production remains unchanged by this review.
