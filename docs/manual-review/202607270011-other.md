# Manual Review Packet

Generated: `2026-07-29T19:24:57Z`

Version: `202607270011`
Stream: `other`
Module: `transaction_network`
Original file: `202607270011_attorney_transaction_key_dates.sql`
Original action: `manual_data_review`
Current decision: `apply_original_after_dependency_check`

## Why This Is Gated

This migration adds attorney-controlled key-date columns to `public.transactions`. The Phase 5 catalog audit classified it as `no_static_objects`, because the extractor does not treat additive column-only DDL as a static object set for live comparison. It therefore needs an explicit schema review before it can be applied or repaired.

## Intended Outcome

- Add instruction, agreement, obligation, finance clause, transfer duty, lodgement, expected lodgement, and expected registration date fields.
- Preserve existing rows by using `add column if not exists`.
- Add comments for the attorney-controlled obligation, transfer duty, and lodgement dates.

## Required Review Before Clearance

- Diff the live `public.transactions` columns for every added column in this migration.
- Confirm any existing columns have the same type as the migration expects.
- Confirm there is no conflicting attorney key-date migration already applied under another version.
- Run transaction detail, attorney workspace, and CRM smoke after staging.

## Phase 20 Live Review

Read-only production audit on `2026-07-29T19:32:16Z` found:

- Ledger row recorded: `false`
- Expected key-date columns: `17`
- Columns already present with the expected type: `1`
- Columns missing: `16`
- Existing correct column: `target_registration_date`

The original migration is safe to route to staging because every DDL operation uses `add column if not exists`, and the only already-present column matches the expected `date` type.

## Phase 20 Decision

Clear for the staging runner as:

`apply_original_after_dependency_check`

Staging evidence must still prove the schema diff and the transaction detail, attorney workspace, and CRM behavior checks before production promotion.

## Blockers

- None
