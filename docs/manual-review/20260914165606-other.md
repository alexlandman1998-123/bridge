# Manual data review — activate canonical FICA declaration rules

Migration: `20260914165606_fica_declaration_canonical_pack_activate_rules.sql`  
Reviewed and approved by: Alex

The preceding migration creates the five fixed FICA declaration rule IDs but,
on a first insert, leaves `is_active` at the table default. This corrective
migration first asserts that all five expected IDs and identity tuples exist,
then sets only those rows active. It is idempotent and does not alter customer
documents, case data, or review outcomes.

Decision: `apply_original_after_dependency_check`.
