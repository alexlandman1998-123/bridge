# Manual data review — activate canonical FICA certificate rule

Migration: `20260914172028_fica_compliance_certificate_activate_rule.sql`  
Reviewed and approved by: Alex

The preceding certificate migration creates the fixed canonical rule ID
`00000000-0000-4000-8000-000000000106`. On a first insert it inherits the
inactive table default. This correction asserts that exact identity tuple and
activates only that row. It is idempotent and does not alter certificates,
case records, documents, or provider data.

Decision: `apply_original_after_dependency_check`.
