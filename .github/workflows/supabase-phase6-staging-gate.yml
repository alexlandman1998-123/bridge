name: Supabase Phase 6 Staging Gate

on:
  pull_request:
    paths:
      - 'scripts/supabase-phase5-module-drift-audit.mjs'
      - 'scripts/supabase-phase6-staging-execution.mjs'
      - 'scripts/supabase-phase6-staging-execution.test.mjs'
      - 'docs/supabase-phase-5-application-manifest.json'
      - 'docs/supabase-phase-6-implementation-status.md'
      - 'package.json'
      - '.github/workflows/supabase-phase6-staging-gate.yml'
  push:
    branches:
      - main
    paths:
      - 'scripts/supabase-phase5-module-drift-audit.mjs'
      - 'scripts/supabase-phase6-staging-execution.mjs'
      - 'scripts/supabase-phase6-staging-execution.test.mjs'
      - 'docs/supabase-phase-5-application-manifest.json'
      - 'docs/supabase-phase-6-implementation-status.md'
      - 'package.json'
      - '.github/workflows/supabase-phase6-staging-gate.yml'

permissions:
  contents: read

jobs:
  staging-gate:
    name: Verify fail-closed staging execution
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Test staging execution gate
        run: node scripts/supabase-phase6-staging-execution.test.mjs

      - name: Verify manifest coverage
        run: |
          node <<'NODE'
          const { spawnSync } = require('node:child_process')
          const manifest = require('./docs/supabase-phase-5-application-manifest.json')
          if (manifest.linkedProjectRef !== 'isdowlnollckzvltkasn') throw new Error('Unexpected manifest project ref')
          if (!Array.isArray(manifest.rows) || manifest.rows.length === 0) throw new Error('Manifest rows are missing')
          const plan = spawnSync(process.execPath, ['scripts/supabase-phase6-staging-execution.mjs', '--plan', '--json'], { encoding: 'utf8' })
          if (plan.status !== 0) throw new Error(plan.stderr || 'Phase 6 plan failed')
          const parsed = JSON.parse(plan.stdout)
          if (!Number.isInteger(parsed.count) || parsed.count <= 0) throw new Error('Phase 6 plan count is invalid')
          if (parsed.count > manifest.rows.length) throw new Error('Phase 6 plan count exceeds manifest rows')
          NODE
